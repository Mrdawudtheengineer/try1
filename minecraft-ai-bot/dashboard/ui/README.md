# Dashboard UI (Vite + React)

Development:

cd dashboard/ui
npm install
npm run dev

Production build (outputs into dashboard/public):

npm run build

The dashboard server serves files from `dashboard/public` on port 3000.